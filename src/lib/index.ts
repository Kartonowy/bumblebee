// place files you want to import through the `$lib` alias in this folder.
export type Card = {
    name: string | null;
    url: string | null;
    rank: string | null;
    series: string | null;
    explaination: string | null;
}